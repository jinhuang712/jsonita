# 用户使用约法三章

## 1. 不确定时提供选项
当遇到不确定的地方，请及时提供选项并索要答案，不要猜测。

## 2. 节省 Token 使用
- 绝对禁止全局 grep 搜索。
- 绝对禁止对超大文件执行 cat 操作。
- 优先使用精准文件搜索和小范围读取。

## 3. Git 操作规范
- 只有 `~/dev/repository` 下的仓库才使用 yummy / `ym` 提交。
- 当前项目不在 `~/dev/repository` 下，不能使用 `ym`。
- 每次完成代码或文档改动后都要提交；不确定该用哪种提交方式时，先给出选项并询问。

## 4. 实现与文档同步
- 所有实现改动必须同步检查并更新 `spec/` 与 `plan/` 中对应章节。
- 不允许让 `spec/` / `plan/` 描述已经偏离当前实现的行为。
- 完成实现后需要重启 Jsonita 应用，确保用户可直接用 `Cmd+Shift+J` 验证。
