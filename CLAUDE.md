# CAST Agent Instructions

This repository uses CAST workflow policy. Resolve `<repo-root>` as this
checkout, then read and follow `.cast-docs/WORKFLOW.md` before doing repository
work.

Non-negotiable bootstrap rules:

1. Do not proceed without loading `.cast-docs/WORKFLOW.md`.
2. Avoid broad global searches and large file dumps; use targeted reads.
3. This repo is outside the internal repository root that requires
   `yummy` / `ym`, so use direct `git -C <repo-root>` commands.
4. If a decision is unclear and affects scope, architecture, rewrite depth, or
   implementation order, offer concrete options and ask instead of guessing.
